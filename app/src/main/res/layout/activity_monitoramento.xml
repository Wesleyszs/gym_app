<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <CalendarView
        android:id="@+id/calendarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Status da Consulta"
        android:textSize="18sp"
        android:gravity="center"
        android:padding="16dp" />

    <TextView
        android:id="@+id/textViewConsultaInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Informações da Consulta"
        android:textSize="16sp"
        android:gravity="center"
        android:padding="16dp" />

    <LinearLayout
        android:id="@+id/consultaMarcadaLegend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:visibility="gone">
        <!-- Outros componentes para consulta marcada -->
    </LinearLayout>

    <LinearLayout
        android:id="@+id/indisponivelLegend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:visibility="gone">
        <!-- Outros componentes para data indisponível -->
    </LinearLayout>
</LinearLayout>
